using namespace std;
#include "Free_Fonts.h"
#include <WiFi.h>
#include <M5Stack.h>
#include <string>


WiFiClient client;
int status = WL_IDLE_STATUS; // the Wifi radio's status

char ssid[]     = "AP";
char password[] = "lavit2ap_";

void setup() {
  Serial.begin(115200);
  delay(100);
  Serial.print("Connecting to: ");
  Serial.println(ssid);
  WiFi.begin(ssid, password);

  while (WiFi.status() != WL_CONNECTED) {
      delay(500);
      Serial.print(".");
  }
  
  M5.begin();
  Serial.println("");
  Serial.println("Connected.");
  M5.Lcd.println("");
  M5.Lcd.println("Connected.");

}

void loop() {

}
